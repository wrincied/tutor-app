# Firebase App Hosting — Angular SPA (tutor-app)
# https://tutor-app--tutorassis.europe-west4.hosted.app
runConfig:
  minInstances: 0
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 512

# Секреты не нужны для статической сборки; Firebase config в environment.prod.ts.
