# Статические ресурсы (Angular `public/`)

Файлы из этой папки доступны в приложении по URL вида `/assets/...`.

Примеры:
- `/assets/icons/icon-close.svg`
- `/assets/icons/icon-warning.svg`

При `ng build` содержимое копируется в `dist/tutor/browser/`.
