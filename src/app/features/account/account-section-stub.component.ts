import { Component } from '@angular/core';

/** Keeps /account/:section URLs active while content lives in the shell. */
@Component({
  selector: 'app-account-section-stub',
  standalone: true,
  template: '',
})
export class AccountSectionStubComponent {}
