/** Событие: открылся другой оверлей (dialog/modal) — закрыть открытые select. */
export const APP_OVERLAY_LAYER_OPEN = 'app-overlay-layer-open';

/** На html: выпадающий select открыт — максимальный z-index для меню */
export const APP_SELECT_DROPDOWN_OPEN_CLASS = 'app-select-dropdown-open';

/** Корень для portal select — body, чтобы fixed не резался transform модалки */
export const APP_SELECT_PORTAL_ROOT = 'body';
